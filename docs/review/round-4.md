# Review round 4 (workflow wf_1fb67f61-747)

Confirmed 7, refuted 0.

## [medium] slice-cvcr4-01: REGRESSION (slice-cv-04, broken by the slice-cvcr3-01 fix): the footer highlight 'motorsport / F1, music, esports →' turns muted #5f5b54 on hover and keyboard focus
- file: src/components/Highlight.module.css:21 (checklist cv-25 / cv-15)
- expected: The footer highlight text stays #1c1b19 (rgb(28, 27, 25)) at rest, on hover and on focus. Only the background changes, to rgba(255,196,40,.95). In the prototype the footer span has inline color:#1c1b19, and its style-hover changes only the background.
- actual: At rest it is rgb(28, 27, 25), which is correct. On hover and on :focus-visible it computes to rgb(95, 91, 84). The round-3 fix added `color: inherit` to `.yellow:hover, .yellow:focus-visible` (Highlight.module.css:18-22), with specificity (0,2,0). That beats `.footer a { color: var(--ink) }` (CvSheet.module.css:134-136, specificity (0,1,1)), so the link inherits the footer paragraph's muted colour (CvSheet.module.css:128-129). The other three yellow highlights are unaffected because their parent (.entryTitle) inherits ink.
- fix: In src/components/sheets/CvSheet.module.css:134, change the selector to `.footer a, .footer a:hover, .footer a:focus-visible { color: var(--ink); }` (specificity (0,2,1) outranks .yellow:hover). Add an e2e test that hovers and focuses the footer highlight and asserts color rgb(28, 27, 25).

## [low] slice-cvcr4-02: The Crumbify GitHub CTA label and the back-pill label are hardcoded in components, not read from src/content.ts (the ctas[1].label and icon fields are dead)
- file: src/components/sheets/CrumbifyCtas.tsx:24 (checklist extra-bd-04 / cr-10 / cr-12)
- expected: extra-bd-04 says all copy lives in one content module (src/content.ts). The CTA row should render crumbify.ctas[1].label (and the icon from crumbify.ctas[1].icon), the same way the App Store CTA renders crumbify.ctas[0].label.
- actual: CrumbifyCtas.tsx:24 renders the literal 'View on GitHub' and always renders <GithubIcon> (line 23). Nothing reads content.ts:195 `{ label: 'View on GitHub', ..., icon: 'github' }`'s label or icon, so editing the copy there has no effect on the page. BackPill.tsx:6 also hardcodes '← back to CV', which appears nowhere in content.ts. The rendered text is currently correct.
- fix: src/components/sheets/CrumbifyCtas.tsx:23-24: replace them with `{crumbify.ctas[1]?.icon === 'github' && <GithubIcon size={20} />}{crumbify.ctas[1]?.label}`. Add `backToCv: '← back to CV'` to an exported site/ui object in src/content.ts and render it in src/components/BackPill.tsx:6 instead of the literal.

## [low] slice-rv-gps-crop-01: Racing GPS polaroid shows a different crop from the reference: the view is shifted about 10% of the image width to the right and 2% up, so the browser bar and the dark side panel show and the track is not centred
- file: src/content.ts:246 (checklist rv-11)
- expected: The crop from the reference image-slot (s:1.8, x:+18, y:+50, applied the way reference/image-slot.js _applyView does it at lines 1034-1040: the image is sized to cover x scale, and its centre sits at left 50+x %, top 50+y % of the frame). For the 161x348 asset in a 300x300 frame, the visible window covers image x 12.2%-67.8% and image y 24.3%-50.0% (centre 40% / 37.15%). This is the track-centred crop the prototype renders, with no browser toolbar at the top.
- actual: imageCrop { scale: 1.8, objectPosition: '58% 22%' } (content.ts:246), applied in RacingSheet.tsx:37 as object-fit:cover plus transform:scale(1.8) around the frame centre. The visible window is image x 22.2%-77.8% and y 22.1%-47.8% (centre 50% / 35%). The object-position x value has no effect, because under cover the image width equals the frame width. The render shows the laptop's browser tab/address bar across the top, the dark right-hand panel with its orange text, and the blue track in the left third instead of the centre.
- fix: In src/content.ts:246 change imageCrop to { scale: 1.8, objectPosition: '50% 26.1%', translateX: '10%' } and add translateX to its type. In src/components/sheets/RacingSheet.tsx:37 apply style={{ objectPosition, transform: `scale(${scale}) translateX(${translateX})` }}. The translate runs in pre-scale units, 10% of the frame width, which equals 10% of the image width under cover, so the view centre moves from image x 50% to 40%. Object-position y 26.1% moves the centre to (50+116*0.261)/216 = 37.15%. The result matches the reference window of x 12.2-67.8% and y 24.3-50.0%. Also update the comment at content.ts:238-245 and the deviation entry at docs/implementation-notes.md:19 so they describe the exact mapping of the reference pan (s 1.8, x +18, y +50).

## [low] behaviour-NOT-FIXED-01: NOT-FIXED (behaviour-03): the reduced-motion sheet swap is still a hard cut, not a cross-fade. The incoming sheet's opacity never meaningfully drops below 1
- file: src/lib/pile.ts:102 (checklist bh-13)
- expected: README Accessibility and bh-13: under prefers-reduced-motion, a sheet swap is an instant cross-fade. The incoming sheet should visibly fade from about 0 to 1 over Sheet.module.css's `transition: opacity 0.2s`, and some frame should show 0 < opacity < 1 (this was the round-3 fix's own acceptance criterion).
- actual: The round-3 fix (da95c86) starts the move at stage 'out' with inline opacity 0 and switches to 'in' (opacity 1) 16ms later. But a hidden sheet's resting style (sheetStyle's last branch, pile.ts:102) has opacity 1. Going hidden to 'out' therefore starts a 1 -> 0 transition that is only about 16ms in when 'in' reverses it back to 1. Measured live at 1440x900 with reducedMotion:'reduce', sampling getComputedStyle(sheet).opacity every rAF: racing = 1.0000 at every frame (z 0 -> 9 -> 11 -> 10), video = 1.0000 at every frame, about = a single frame at 0.7787, then 0.9914, then 1. The same happens after a live switch to reduced motion (emulateMedia mid-session): racing stays at 1.000 in every frame. The incoming sheet goes from under the top sheet (z 9) to over it (z 11) at full opacity, so it is still a cut. Unit tests pass because they check only the inline style values, and no e2e test samples the computed opacity.
- fix: In src/lib/pile.ts sheetStyle, change the hidden branch (line 101) to `return { transform: STRAIGHT_TRANSFORM, z: 0, visible: false, opacity: reducedMotion ? 0 : 1 };` so a hidden sheet already rests at opacity 0 under reduced motion. The 'out' -> 'in' switch then runs a real 0 -> 1 fade over 0.2s. The 'in' sheet is at z m+3, above the outgoing sheet, so this is a true cross-fade. Make sure the outgoing sheet, which becomes hidden at settle, is not visibly faded, since it is already covered. Update the pile.ts unit tests for the hidden-branch opacity. Add an e2e test under emulateMedia({reducedMotion:'reduce'}) that samples getComputedStyle(#sheet-<k>).opacity on each rAF after a tab click and asserts that at least one frame with visibility 'visible' has 0.05 < opacity < 0.95.

## [low] code-r4-01: NOT-FIXED (behaviour-03): the reduced-motion sheet swap is still a hard cut; the incoming sheet never shows an opacity below 1
- file: src/lib/pile.ts:101 (checklist bh-13)
- expected: Under prefers-reduced-motion, bring() cross-fades the incoming sheet: it starts at opacity 0 and fades to 1 over Sheet.module.css's `transition: opacity .2s`, so some frame shows 0 < opacity < 1 (this is the round-3 behaviour-03 fix, commit da95c86).
- actual: A hidden sheet gets opacity 1 from sheetStyle's fallback branch (pile.ts:101). The 'out' stage then sets inline opacity 0 (pile.ts:93), which only starts a 1 -> 0 transition. REDUCED_OUT_MS = 16 ms later (usePile.ts:37 and 168), the 'in' stage sets opacity back to 1 and z to m+3. That cancels the barely-started transition, so the sheet lands on top at full opacity in one frame. On the built site (reduced motion, /#cv, click the 'crumbify' tab) every rAF sample of getComputedStyle(#sheet-crumbify).opacity reads '1', including the frames in the 'out' stage. The unit test at src/lib/usePile.test.tsx:269 only checks the state stages ('out' then 'in' after 16 ms), so it passes on this broken behaviour.
- fix: In src/lib/pile.ts sheetStyle, make the final hidden branch (line 101) return `opacity: reducedMotion ? 0 : 1`, so a hidden sheet already sits at opacity 0 and the 'out' stage causes no transition. In src/lib/usePile.ts useBring, switch 'out' to 'in' only after the 'out' style has been committed: use a double requestAnimationFrame instead of at(REDUCED_OUT_MS), or keep the 16 ms timer if pile.ts is fixed, since the hidden->out change no longer changes opacity. The 'in' stage (opacity 1) then runs the full 0->1 fade from Sheet.module.css's `transition: opacity 0.2s`. Add a Playwright test with reducedMotion:'reduce': click the crumbify tab, sample getComputedStyle(#sheet-crumbify).opacity each rAF for 400 ms, and assert that some sample has visibility 'visible' and 0 < opacity < 1.

## [low] code-r4-02: Two round-3 CSS fixes have no regression test: reverting either one still passes every unit and e2e test
- file: src/components/Highlight.test.tsx:59 (checklist cv-18)
- expected: Round-3 fixes slice-cvcr3-01 (yellow highlight keeps ink #1c1b19 on hover, via `color: inherit` in `.yellow:hover` at Highlight.module.css:18-22) and slice-new-01 (no outline on the programmatically focused sheet, via `.sheet:focus, .sheet:focus-visible { outline: none }` at Sheet.module.css) each have a test that fails if the fix is removed. The round-3 fix text asked for a Playwright hover test on 'Founder & Lead Engineer'.
- actual: Highlight.test.tsx:59-66 checks with a regex only that the `.purple:hover` block has color:inherit. No test covers `.yellow:hover`. No unit or e2e test checks the sheet container's outline: the only outline assertion (e2e/pile.spec.ts:357-366) is for the CV contact link. Removing `color: inherit;` from `.yellow:hover` (which brings back the #8a5a00 hover text) or deleting the `.sheet:focus` rule leaves all 114 unit tests and every e2e test green. The fixes themselves work on the current build: hover colour is rgb(28, 27, 25), and #sheet-crumbify has outline-style 'none' after keyboard activation.
- fix: In src/components/Highlight.test.tsx, add a test next to the purple one: match `/\.yellow:hover,\s*\n?\s*\.yellow:focus-visible\s*{([^}]*)}/` and expect the captured block to match /color:\s*inherit/. In e2e/pile.spec.ts, add: (1) hover the 'Founder & Lead Engineer' highlight and expect getComputedStyle color 'rgb(28, 27, 25)'; (2) focus that highlight, press Enter, wait for #sheet-crumbify to be document.activeElement after the move settles, and expect getComputedStyle(activeElement).outlineStyle to be 'none'.

## [low] code-r4-03: The Crumbify GitHub CTA hardcodes its label in the component and ignores content.ts; the `icon` fields in content are dead
- file: src/components/sheets/CrumbifyCtas.tsx:24 
- expected: content.ts is the single typed content module ('All copy + URLs for the site live here', content.ts:2). Components read their copy from it, and content fields are either used or removed.
- actual: CrumbifyCtas.tsx:24 renders the literal 'View on GitHub' rather than crumbify.ctas[1].label. That content field (content.ts, crumbify.ctas[1]) is never read, so editing it has no effect. The `icon: 'github'` fields on crumbify.ctas[1] and video.cta are also never read: CrumbifyCtas.tsx:23 and VideoSheet.tsx:38 always render <GithubIcon>. Only the CV project's icon field (CvProjects.tsx:21) is used.
- fix: In src/components/sheets/CrumbifyCtas.tsx line 24, replace the literal `View on GitHub` with `{crumbify.ctas[1]?.label}`. Then either render <GithubIcon size={20} /> only when `crumbify.ctas[1]?.icon === 'github'` (CrumbifyCtas.tsx:23) and `video.cta.icon === 'github'` (VideoSheet.tsx:38), or delete the unused `icon` fields from crumbify.ctas[1] and video.cta in src/content.ts.

## Refuted

