# Review round 7 (workflow wf_af58503a-496)

Confirmed 9, refuted 0.

## [medium] slice-r7-01: REGRESSION (behaviour-03 fix): at 540-599px the sticky note is pushed up to top:-48px into the mobile tab row, and the ABOUT/VIDEO BOT pills cover the note's opening words 'psst: c'
- file: src/components/StickyNote.module.css:216 (checklist atn-22 / atn-24 / slice-rvat5-01)
- expected: The note copy 'psst: click anything highlighted (or a tab) to pull that page from the pile.' is fully readable at every width. The behaviour-03 fix was only meant to move the note clear of the CV's github link, without creating a new overlap. At 539px (top:-22px) the pills overlap only the note's top padding and the text is clear.
- actual: The band `@media (min-width:540px) and (max-width:660px){.note{top:-48px;right:8px}}` also applies at 540-599px. At those widths the <600px mobile tab row (Tabs.module.css:146-147, position:relative; z-index:1, which paints above the whole .stage) sits directly above the stage. Lifting the note by another 26px puts its first text line under the pills. At 560px the note box is [318,74,536,204] and the ABOUT pill is [335,91,396,117], so the pill paints over 'psst: c'. At 580px the ABOUT pill hides 'psst', and at 599px the note's left edge is x=357 while the pill runs to x=396. At 600-660px, where the tab row is not shown, the moved note is fine, and from 661px up the github link is clear, so behaviour-03 itself is fixed.
- fix: In src/components/StickyNote.module.css, change the existing band to `@media (min-width:600px) and (max-width:660px){ .note{top:-48px;right:8px} }`. For 540-599px, where the mobile tab row sits directly above .stage, choose a position that stays below the tab row (note top >= tab-row bottom, i.e. note top offset >= -22px relative to .stage) and also does not overlap the CV contact row's github link. Either keep top:-22px and force the CV contact row to wrap the github link onto its own line below 600px, as it already does at <=539px, or add a 540-599 band with a smaller right offset/width so the note ends left of x=387. Add an e2e test at 560px and 590px, /#cv: the note's first text line rect must not intersect any visible nav[aria-label=Sheets] button rect, AND the note box must not intersect the github.com/AliBars19 link rect.

## [medium] code-r7-01: The vi.doMock + dynamic import() unit tests in CrumbifyCtas.test.tsx and CvProjects.test.tsx time out under parallel load, and the late render then breaks the next test
- file: src/components/sheets/CrumbifyCtas.test.tsx:26 
- expected: The unit test gate (npm test) is deterministic. Every test passes whenever the code is correct, whatever the machine load.
- actual: Each test calls vi.resetModules() and vi.doMock('@/content'), then runs `await import('./CrumbifyCtas')` (lines 26/43/58/73/88), or `await import('./CvProjects')` in CvProjects.test.tsx (lines 35/62/86). So every test transforms and evaluates the component's module graph again from cold, inside the default 5000ms test timeout. Under load, the first one or two tests in each file go over the limit with 'Error: Test timed out in 5000ms'. The timed-out test's async body keeps running and renders into the DOM after the next test has started. The next test then fails with 'TestingLibraryElementError: Found multiple elements with the role "link" and name /View on GitHub/' (or /Automated Publishing Platform/). This matches the intermittent CrumbifyCtas.test.tsx failure the fix writer reported. The same file pattern is also in VideoSheet.test.tsx.
- fix: In src/components/sheets/CrumbifyCtas.test.tsx, CvProjects.test.tsx and VideoSheet.test.tsx, stop cold-importing inside each test. Use `const fixture = vi.hoisted(() => ({ crumbify: { ctas: [] as Cta[] } }))` with a top-level `vi.mock('@/content', () => fixture)`. Import the component statically at the top of the file. In each test, assign `fixture.crumbify.ctas = [...]` before render(). Remove vi.resetModules() and vi.doUnmock(). This works because the components read content at render time. If a component reads content at module load, move that read into the render body. As a minimum stop-gap, if any dynamic import remains, add `{ timeout: 20000 }` and call cleanup() in afterEach.

## [low] slice-01: REGRESSION (behaviour-03 fix): from 540 to 599px the sticky note is pushed up under the mobile tab row, so the ABOUT / VIDEO BOT pills cover the start of its text ('psst: c...')
- file: src/components/StickyNote.module.css:44 (checklist extra-gl-34 / pile-20 / pile-21 (related atn sticky note, gl-26))
- expected: The sticky note hint (z-index 90) is fully readable and not covered by other UI. The round-6 behaviour-03 fix was meant only to move the note clear of the CV contact row. Before that fix (top:-22px; right:28px), the note's first text line sat at y=120-142 at 540-599px, below the mobile tab row (y=91-117), and no tab covered its text.
- actual: The new `@media (min-width: 540px) and (max-width: 660px) { .note { top: -48px; right: 8px; } }` rule also applies inside the mobile layout (<600px). There the tab row is a flow sibling directly above .stage and has position:relative; z-index:1, so it paints above the whole stage, note included. At 540-599px the note's first text line moves up to y=94-116 and sits under the tab pills. At 560px the ABOUT pill (x 335-396, y 91-117) hides 'psst: c'. At 540px the VIDEO BOT pill hides the start of the line. The 600-660px part of the band is fine: the note sits below the header and does not cover it. Widths 360-539px are unaffected.
- fix: In src/components/StickyNote.module.css:44, change the query to `@media (min-width: 600px) and (max-width: 660px)` and keep `top: -48px; right: 8px`. That value is verified clean on desktop. Add a second rule, `@media (min-width: 540px) and (max-width: 599px) { .note { top: -24px; right: -8px; } }`. With it, the note's first text line clears the mobile tab row (text top >= 118px, row bottom 117px), and GitHub-link overlap drops to at most 9 of 140 sample points at 560px (0 at 540px and 599px). Confirm that `right:-8px` causes no horizontal scroll at 540-599px; I measured scrollWidth equal to the viewport width at 540px and 599px. Add an e2e test at 540, 560 and 599px. It should temporarily set the note to pointer-events:auto, build a Range over the note's first text line, and assert that elementFromPoint across that range never resolves inside nav[data-variant=mobile]. Keep the existing behaviour-03 GitHub-link assertion green at the same widths.

## [low] slice-cvcr-flaky-01: CrumbifyCtas.test.tsx and CvProjects.test.tsx fail intermittently: the first test does a cold dynamic import that hits the 5s timeout, and the late render then breaks later tests
- file: src/components/sheets/CrumbifyCtas.test.tsx:17 (checklist cr-12 / extra-bd-05)
- expected: Unit tests for the TODO-placeholder CTA and title rules pass every run.
- actual: 3 of 5 runs of `npx vitest run CrumbifyCtas.test.tsx CrumbifySheet.test.tsx CvSheet.test.tsx CvProjects.test.tsx Highlight.test.tsx` failed. In a further 3 runs, 2 failed. Every failure starts with the first test in a file giving 'Error: Test timed out in 5000ms.' (CrumbifyCtas.test.tsx:17, and also CvProjects.test.tsx:19). The later tests in the same file then fail with 'TestingLibraryElementError: Found multiple elements with the role "link" and name /Get it on the App Store/' (or /View on GitHub/). The imports that timed out resolve late and render into the next test's document.
- fix: In src/components/sheets/CrumbifyCtas.test.tsx and src/components/sheets/CvProjects.test.tsx, give the dynamic-import tests enough time and keep the cascade from happening. (1) Add a longer timeout for these files: either call `vi.setConfig({ testTimeout: 20000 })` at the top of each describe block, or pass `20000` as the third argument of each `it(...)`. The alternative is `testTimeout: 15000` in the vitest.config.ts `test` block. (2) Import `cleanup` from '@testing-library/react' and call `cleanup()` as the first line of each afterEach, before `vi.resetModules()`. Optionally, also warm the module graph once with `beforeAll(async () => { await import('./CrumbifyCtas'); }, 30000)`, and the same for './CvProjects', so the first test does not pay the cold-transform cost inside its timeout. To verify, run the 5-file command 10 times in a row and confirm 0 failures.

## [low] slice-r7-02: Divider tab accessible names come out upper-case ('CV', 'CRUMBIFY', 'VIDEO BOT') and not the labels 'cv' / 'video bot', because the prototype's aria-label was dropped
- file: src/components/Tabs.tsx:56 (checklist atn-20)
- expected: atn-20: each tab is a <button> whose accessible name equals its label: 'cv', 'crumbify', 'racing', 'video bot', 'about'. The prototype sets `aria-label="{{ tb.label }}"` on each tab button (reference/Interactive CV v3.dc.html:193).
- actual: The buttons have no aria-label, so Chromium builds the name from the rendered text, which has `text-transform: uppercase` applied (Tabs.module.css:95 and :157). Chrome's accessibility tree names the tabs 'CV', 'CRUMBIFY', 'RACING', 'VIDEO BOT', 'ABOUT', on both the desktop and the mobile variant. jsdom unit tests ignore text-transform, so they do not catch this.
- fix: In src/components/Tabs.tsx, add `aria-label={tab.label}` to the tab <button> (desktop and mobile variants share this element). Add an e2e assertion that reads Chromium's AX tree names, via CDP Accessibility.getFullAXTree or page.accessibility.snapshot(), and expects exactly ['cv','crumbify','racing','video bot','about'] for the Sheets tabs.

## [low] behaviour-r7-01: REGRESSION (from the behaviour-03 fix): from 540 to 599px wide, the mobile tab row now hides the start of the sticky note's text ('psst: click')
- file: src/components/StickyNote.module.css:44 (checklist bh-17 / atn-27 / gl-26)
- expected: The note copy 'psst: click anything highlighted (or a tab) to pull that page from the pile.' can be read in full at every width (bh-17: content never clips). The behaviour-03 fix should move the note clear of the CV's GitHub link without pushing it under anything else. Below 540px the note uses the spec's top:-22px, and there the tab row only overlaps the note's 18px top padding, so no text is hidden (checked at 520px: every first-line character hit-tests to the sheet, none to a tab).
- actual: The new rule `@media (min-width: 540px) and (max-width: 660px) { .note { top:-48px; right:8px } }` also applies in the 540-599px mobile layout, where the divider tabs are a row directly above the stage. That row has position:relative; z-index:1 (the slice-rvat5-01 fix), so it paints over the note. Moving the note up 26px puts its first text line behind the 'about' (and at 540px also the 'video bot') tab pill. Characters hidden behind a tab button (elementFromPoint at each character's centre): 540px 'psst: click' (11 chars), 560px 'psst: c', 580px 'psst', 599px 'ps'. There is also no need for the fix below 560px: at 540px the GitHub contact link already wraps onto its own line, as it does at 520px.
- fix: In src/components/StickyNote.module.css, change line 44's band to `@media (min-width: 600px) and (max-width: 660px)` (keep top:-48px; right:8px there). Add a mobile band `@media (min-width: 540px) and (max-width: 599px) { .note { top: -30px; right: 0; } }`. At 540, 560, 580 and 599px I measured this as 0 note characters under a nav[data-variant=mobile] button, 0 GitHub-link sample points covered by the note, and no horizontal overflow. Add an e2e test at 540, 560, 580 and 599px with two checks: for every character of the note text, elementFromPoint at its centre is never inside a mobile tab button; and the GitHub link's rect does not meet the note's rect (elementFromPoint with the note's pointer-events temporarily set to auto).

## [low] code-r7-02: The reduced-motion cross-fade e2e test is flaky: it reads opacity once after a fixed 50ms sleep, and gets 1 or 0 under load
- file: e2e/pile.spec.ts:505 (checklist bh-13)
- expected: The test proves that the incoming sheet passes through a partial opacity, and it does so reliably, whatever the machine load.
- actual: The test clicks the tab, runs page.waitForTimeout(50), and reads getComputedStyle(opacity) once. Whether that read lands inside the page's window (the 'in' stage switches at about 16ms and the fade ends at about 216ms) depends on scheduling. Under load the read sometimes comes back as 1 because it lands after the fade has finished. It also sometimes comes back as 0 because the 'in' stage has not been committed yet.
- fix: In e2e/pile.spec.ts around line 492, replace the waitForTimeout(50) single read with sampling inside the page. Before the click, run `await incoming.evaluate(el => { (window as any).__op = []; const tick = () => { (window as any).__op.push(Number(getComputedStyle(el).opacity)); if ((window as any).__op.length < 60) requestAnimationFrame(tick); }; requestAnimationFrame(tick); })`. Then click, then `await page.waitForFunction(() => (window as any).__op.length >= 60)`. Then assert that some sample v satisfies 0 < v < 1. Alternatively, assert that a 'transitionrun' event fires on #sheet-crumbify with propertyName 'opacity'.

## [low] code-r7-03: The mobile tab loop e2e test can count 0 tabs: toBeVisible() passes while the row is still aria-hidden before hydration
- file: e2e/pile.spec.ts:548 (checklist slice-rvat5-01)
- expected: The test waits until the mobile tab buttons are exposed before counting them, so the `count > 0` check cannot fail on timing alone.
- actual: Tabs.tsx renders the mobile <nav> with aria-hidden and inert, at opacity 0, until usePile reaches phase 'done'. Before hydration the state is the SSR 'off' phase. Playwright's toBeVisible() treats opacity:0 as visible, so `await expect(nav).toBeVisible()` passes on the pre-hydration HTML. `nav.getByRole('button').count()` excludes aria-hidden elements, so it returns 0 if it runs before React has hydrated and applied the skip. This matches the reported intermittent failures of the 520/560/599px tab-width loop.
- fix: In e2e/pile.spec.ts:547, replace `await expect(nav).toBeVisible()` with `await expect(nav).not.toHaveAttribute('aria-hidden', 'true')`, followed by `await expect(nav.getByRole('button')).toHaveCount(5)`, before calling count(). Make the same change to any other test that gates on the mobile nav's toBeVisible(), such as the 390px layout test near line 58.

## [low] code-r7-04: The e2e check that the sticky note does not visually cover a mobile tab cannot fail any more, because the note now has pointer-events:none
- file: e2e/pile.spec.ts:580 (checklist slice-rvat5-01)
- expected: The comment at e2e/pile.spec.ts:570-575 says what this check guards: 'what matters is which element actually wins the paint wherever the two boxes overlap... require the tab, not the note, to be on top there'. It should fail if the note is painted over a tab.
- actual: The round-6 fix for behaviour-03 added `pointer-events: none` to .note (StickyNote.module.css:28). document.elementFromPoint skips elements with pointer-events:none, so it always returns whatever is under the note. The check therefore passes even when the note fully covers every tab. The slice-rvat5-01 regression it was written to catch (the mobile tab row losing its `position:relative; z-index:1` in Tabs.module.css) would no longer be detected. The same blindness applies to the new GitHub-link tests (lines 605-628). They now only verify pointer-events and not visual coverage. The only other guard on the 540-660px nudge is a regex in StickyNote.test.tsx that checks the media query exists.
- fix: In e2e/pile.spec.ts, the hit-test loop (~line 553) and the visual-coverage loop (~line 577) need a visual check the note cannot bypass. Inside the page.evaluate, set `note.style.pointerEvents = 'auto'` on the .note element, call elementFromPoint, and restore it to '' in a finally block. Then assert that the tab, not the note, is returned. Keep a separate assertion that getComputedStyle(note).pointerEvents === 'none' for the click-through guarantee. Apply the same temporary pointer-events toggle to the GitHub-link visual check if visual non-coverage is intended there.

## Refuted

