# alibars.dev implementation notes

Spec: `design_handoff_alibars_portfolio/README.md` (high fidelity, authoritative) + `content.json`.

## Decisions (owner, 2026-09-23)
- Stack: Next.js (App Router) + TypeScript + CSS Modules, **npm**, static export (`output: 'export'`). Hosting on Vercel later (owner will link it).
- Missing URLs (App Store, Crumbify repo, video pipeline repo, publishing platform repo) live in ONE content file with `TODO` markers. Buttons still render.
- City Racing: role ends **July 2026** on both the CV and the Racing page; the csg.racing bullet is **removed from the CV** (the Racing page keeps "Here's the team website" link).
- Download CV: `public/Ali_Bars_CV.pdf`, a copy of the source resume with the phone number removed (header line rebuilt and re-centred, links preserved). Generated by PyMuPDF redaction; the original PDF with the phone number is gitignored and must never be committed.
- Copy keeps en dashes in date ranges as the handoff specifies (no em dashes anywhere). The global "Crumbify" frontend rules (no letter-spacing / no en dashes) are Crumbify-app-specific and do not apply here; the handoff's exact values win.
- Intro: plays on every fresh load at `/`; deep links (`/#crumbify` etc.) skip it. Theme persists in localStorage, first visit respects `prefers-color-scheme`.

## Lanes
- Implementation: Codex `gpt-5.6-luna` was out of quota on 2026-09-23 (resets 2026-09-24 17:17), so per the fallback rule the build went to a Claude sonnet writer. Review: Claude opus with a default-REFUTE stance (sol also out of quota).

## Deviations log
- `/#cv`: treated as a deep link (skips the intro, opens the CV directly), matching the acceptance checklist's `extra-intro-21` decision and the orchestration log below. Any hash present on first load, including `#cv`, skips straight to `phase:'done'`.
- Font CSS custom properties (`--font-sans`/`--font-mono`/`--font-serif` in `globals.css`) are applied via a `className` on `<html>` rather than `<body>`. `next/font`'s variable classes (`--font-plex-sans` etc.) were originally on `<body>`; since `--font-serif` is declared on `:root` (`<html>`) and references `var(--font-source-serif)`, and CSS custom-property `var()` substitution resolves once at the *declaring* element (not lazily per-consumer), the reference was guaranteed-invalid at `<html>` and the empty result inherited down, silently falling back to the browser's default serif everywhere. Moving the `next/font` variable classes to `<html>` (same element as the alias tokens) fixes this; confirmed via `getComputedStyle` before/after and a visual screenshot diff against the reference.
- Racing GPS polaroid crop: the reference stores this as `scale 1.8, offset x +18%, y +50%` (a pan value in the design tool's own coordinate system) applied on top of an `object-fit:cover` baseline. That exact pan formula produced a wrong crop against our copy of the asset (a 161x348px phone photo of a laptop screen, already flagged in the handoff as a low-res placeholder to be replaced by Ali) — direction/magnitude didn't translate 1:1 through `object-fit:cover`. Implemented instead via `object-position` (`58% 22%`) plus `transform:scale(1.8)`, tuned by visual comparison against `design_handoff_alibars_portfolio/reference/.image-slots.state.json`'s intent (zoom into the on-screen map, centred on the GPS track, cropping out the bezel/keyboard) rather than the literal offset numbers. Revisit once Ali supplies a higher-res original.
- Sticky note is a child of the `.stage` grid (not the outer `stageWrap`), so its `position:absolute` anchors to the actual pile box. It was previously a sibling of the responsive mobile tab row, which — being out of normal flow — stayed pinned near the top of `stageWrap` and got hidden behind the tab bar at <600px once tabs moved into flex flow. Fixed by nesting it inside `.stage`, which itself moves down under the tab row on narrow viewports. At very narrow widths (down to 390px) the note (210px wide) still visually overlaps part of the CV name/tagline, which reads as intentional given the "sticky note stuck on the page" metaphor and matches the corrected z-order in a screenshot check.

## Orchestration log
- 2026-09-23: Owner turned on ultracode. Acceptance checklist workflow run `wf_3d25cb30-77c` wrote `docs/acceptance-checklist.md` (214 items). Decision: `/#cv` also skips the intro and shows the CV.
